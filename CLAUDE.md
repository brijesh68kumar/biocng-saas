# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

`biocng-saas` is a feedstock-as-a-service platform for biocng. The repository is in its earliest stage — no application code, build system, or tests exist yet.

## Current State

The only file present is `README.md`. There are no commands to build, lint, or test yet. When a stack is introduced, this file should be updated with:

- Build/run/test commands
- Architecture overview (services, data flow, key modules)
- Conventions (code style, branching strategy, environment setup)
